name: RTD Replicated Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      BOX_CLIENT_ID: ${{ secrets.BOX_CLIENT_ID }}
      BOX_CLIENT_SECRET: ${{ secrets.BOX_CLIENT_SECRET }}
      BOX_ENTERPRISE_ID: ${{ secrets.BOX_ENTERPRISE_ID }}
      BOX_PUBLIC_KEY_ID: ${{ secrets.BOX_PUBLIC_KEY_ID }}
      BOX_PRIVATE_KEY: ${{ secrets.BOX_PRIVATE_KEY }}
      BOX_PASSPHRASE: ${{ secrets.BOX_PASSPHRASE }}

    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Cache Python packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r docs/requirements.txt
          pip install sphinx sphinx-rtd-theme  # Add any other required packages

      - name: Install Pandoc
        run: sudo apt-get install -y pandoc

      - name: Install LaTeX for PDF generation
        run: |
          sudo apt-get update
          sudo apt-get install -y texlive texlive-latex-extra texlive-fonts-recommended latexmk

      - name: Build HTML documentation
        run: |
          sphinx-build -b html docs/ docs/_build/html



      - name: Build PDF documentation
        run: |
          sphinx-build -b latex docs/ docs/_build/latex
          cd docs/_build/latex
          latexmk -pdf -quiet -interaction=nonstopmode -f *.tex

      - name: Upload HTML documentation as an artifact
        uses: actions/upload-artifact@v3
        with:
          name: html-docs
          path: docs/_build/html

      - name: Upload PDF documentation as an artifact
        uses: actions/upload-artifact@v3
        with:
          name: pdf-docs
          path: docs/_build/latex/*.pdf
